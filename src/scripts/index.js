const searchEl = document.getElementById('search-book-form');
const addEl = document.getElementById('add-book-form');

searchEl.addEventListener('submit', (event) => {
	event.preventDefault();
});

addEl.addEventListener('submit', (event) => {
	event.preventDefault();
});
